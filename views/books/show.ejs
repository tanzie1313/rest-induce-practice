<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('../partials/head') %>
    <title><%= title %></title>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="teal">
        <div class="nav-wrapper">
            <a href="/" class="brand-logo center">📚 Show Book</a>
        </div>
    </nav>
    <!-- Main Content -->
     <div class="container bottomMargin">
        <div class="card">
            <div class="card content">
                <p><strong>Title:</strong></p><%= book.title %>
                <p><strong>Author:</strong></p><%= book.author %>
            </div>
            <div class="card-action">
                <a href="/books/<%= book.id %>/edit" class="btn waves-effect waves-light teal">Edit</a>
                
                <form action="/books/<%= book.id %>?_method=DELETE" method="POST" style="display: inline">
                    <button type="submit" class="btn red darken-1">Delete Book</button>
                </form>
        </div>
     </div>
</body>
</html>